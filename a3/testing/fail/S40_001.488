% S40
%
% Check that identifier has been declared as a function

begin
    integer foo(integer a, integer b)
    begin
        result (a + b)
    end

    integer x, y, z

    x := 1
    y := 2

    z := bar(x, y)
end
