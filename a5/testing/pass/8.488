begin

    integer a[3]

    a[1] := 0
    a[2] := 0

    loop
        a[1] := a[1] + 1
        loop
            loop
                exit 2 when (a[2] = 2)
                a[2] := a[2] + 1
            pool
        pool
        exit when (a[1] = 2)
    pool

    put a[1], skip
end
